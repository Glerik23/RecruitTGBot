# RecruitTG - Система рекрутингу через Telegram

Система автоматизації процесу найму програмістів через Telegram бота з веб-інтерфейсом (miniapp).

## Особливості

- 🤖 Telegram бот з інтеграцією miniapp
- 👥 Три типи користувачів: кандидат, HR, аналітик, директор
- 🔐 Система ролей з одноразовими запрошеннями
- 📊 Аналітика та статистика для компанії
- 🐳 Docker контейнеризація
- 🌐 Ngrok для розробки

## Технології

- Python 3.11
- FastAPI
- python-telegram-bot
- PostgreSQL
- SQLAlchemy
- Docker & Docker Compose
- Ngrok

## Швидкий старт

1. Скопіюйте `.env.example` в `.env` і заповніть:
   - `BOT_TOKEN` - отримайте у @BotFather
   - `DIRECTOR_TELEGRAM_ID` - ваш Telegram ID (опціонально)
   - `AUTO_ASSIGN_DIRECTOR` - `true` для автоматичного призначення (за замовчуванням `false`)
   - `DB_PASSWORD` - пароль для БД
   - `SECRET_KEY` - випадковий рядок

2. Запустіть:
   ```bash
   docker-compose up -d --build
   ```

3. Відкрийте бота і надішліть `/start`

**Зміна ролі користувача:**

Windows:
```batch
# Скинути роль на кандидата
reset-role.bat <telegram_id> candidate

# Або інша роль
reset-role.bat <telegram_id> hr
reset-role.bat <telegram_id> analyst

# Показати всіх користувачів
list-users.bat
```

Linux/Mac:
```bash
# Через скрипт (в контейнері)
docker-compose exec bot python change_role.py <telegram_id> <role>

# Приклади:
docker-compose exec bot python change_role.py 123456789 candidate
docker-compose exec bot python change_role.py list  # Показати всіх користувачів
```

Детальні інструкції дивіться в `QUICK_START.md`

## Структура проекту

```
RecruitTG/
├── app/
│   ├── bot/          # Telegram бот
│   ├── web/          # Miniapp веб-інтерфейс
│   ├── models/       # Моделі бази даних
│   ├── services/     # Бізнес-логіка
│   └── utils/        # Утиліти
├── main.py           # Точка входу
├── docker-compose.yml
└── Dockerfile
```

## Ліцензія

Приватний проект для технологічної практики.

