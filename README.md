# RecruitTG — Інформаційна система автоматизації рекрутингу через Telegram

Дана система призначена для оптимізації та автоматизації процесів найму персоналу з використанням месенджера Telegram як основного каналу взаємодії та інтегрованого веб-інтерфейсу (Mini App) для розширеної функціональності.

## 🚀 Основні Можливості

Система забезпечує повний життєвий цикл обробки заявок кандидатів:
- **🤖 Telegram Бот**: Основна точка входу, керування ролями та сповіщення.
- **🖥️ Mini App**: Високопродуктивний веб-інтерфейс для заповнення анкет та перегляду аналітики.
- **� Рольова Модель**: Розмежування прав доступу для кандидатів, HR-менеджерів, аналітиків та адміністраторів (директорів).
- **� Аналітична Панель**: Візуалізація ключових метрик (Conversion Funnel, Time-to-Hire тощо).
- **📅 Планування Співбесід**: Інструментарій для погодження часу та форматів інтерв'ю.

## 🛠 Технологічний Стек

Проєкт побудований на сучасних технологіях з дотриманням принципів SOLID та DRY:

### Backend
- **Framework**: FastAPI (Python 3.11)
- **Database**: PostgreSQL
- **ORM**: SQLAlchemy + Alembic (міграції)
- **Library**: `python-telegram-bot`

### Frontend (Mini App)
- **Core**: React 19 + Vite
- **Styling**: Tailwind CSS v4 + Shadcn/UI
- **State Management**: React Hooks & Context API
- **Charts**: Chart.js (через `react-chartjs-2`)

### Infrastructure
- **Containerization**: Docker & Docker Compose
- **Web Tunneling**: Ngrok (для локальної розробки та тестування Webhook)

## 📁 Структура Проєкту

```text
RecruitTG/
├── app/                  # Основний код Backend-додатка
│   ├── bot/              # Логіка Telegram-бота (handler, keyboard)
│   ├── web/              # API роутери та статичні файли Mini App
│   ├── models/           # SQLAlchemy моделі (структура БД)
│   ├── services/         # Бізнес-логіка системи
│   └── schemas/          # Pydantic схеми для валідації даних
├── frontend/             # Вихідний код React-додатка (Vite проект)
├── alembic/              # Файли міграцій бази даних
├── scripts/              # Допоміжні скрипти (автоматизація, зміна ролей)
├── main.py               # Точка входу в FastAPI додаток
└── docker-compose.yml    # Конфігурація середовища розгортання
```

## ⚙️ Швидкий Старт

1.  **Конфігурація середовища**:
    Скопіюйте `.env.example` у `.env` та заповніть необхідні параметри (`BOT_TOKEN`, `DB_PASSWORD`, `SECRET_KEY`).
2.  **Запуск системи**:
    ```bash
    docker-compose up -d --build
    ```
3.  **Ініціалізація**:
    Відкрийте бота та надішліть команду `/start`. Для отримання прав директора скористайтеся автоматичним призначенням (див. `.env`) або використайте скрипт `reset-role.bat`.

Детальні інструкції з налаштування та адміністрування наведені у [QUICK_START.md](file:///d:/%21Projects/RecruitTGBot/QUICK_START.md).

## 📄 Ліцензія

Приватний проєкт, розроблений у межах технологічної практики. Всі права захищені.
